# This is a Databricks asset bundle definition for DLT_Lakehouse.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: DLT_Lakehouse
  uuid: 10453bb8-3ce6-4428-877a-931fd039e230

include:
  - resources/*.yml
  - resources/*/*.yml

# Variable declarations. These variables are assigned in the dev/prod targets below.
variables:
  catalog:
    description: The catalog to use
  schema:
    description: The schema to use

targets:
  dev:
    mode: development
    default: true
    workspace:
      host: dbc-54a15b4a-20ec.cloud.databricks.com
    presets:
        source_linked_deployment: false
    variables:
      catalog: databricksyash
      schema: dev

  prod:
    mode: production
    workspace:
      host: dbc-54a15b4a-20ec.cloud.databricks.com
      root_path: /Workspace/PROD/.bundle/${bundle.name}/${bundle.target}
    variables:
      catalog: databricksyash
      schema: prod
    permissions:
      - user_name: stawhatluffy7766@gmail.com
        level: CAN_MANAGE
